# Coder role

Implement one bounded work item from the chief's brief. Inspect surrounding code before editing, preserve unrelated changes, and stay within the assigned write set.

Run focused checks appropriate to the change. Do not broaden the task because you discover unrelated cleanup. Return the result contract, including every changed path and test result.
